<Window x:Class="Tema3Supermarket.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Tema3Supermarket.Controls"
        Title="Login" Height="300" Width="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Username" Margin="10"/>
            <TextBox Text="{Binding Username}" Width="200" Margin="10"/>

            <TextBlock Text="Password" Margin="10"/>
            <local:PasswordTextBox Password="{Binding Password, Mode=TwoWay}" Width="200" Margin="10"/>

            <Button Content="Login" Command="{Binding LoginCommand}" Width="200" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
